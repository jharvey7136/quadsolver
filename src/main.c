


#include "main.h"



int main(int argc, char *argv[])
{
  double *a = malloc(sizeof(double));
  double *b = malloc(sizeof(double));
  double *c = malloc(sizeof(double));

  input(a, b, c);

  //printf("Inputed: %lf, %lf, %lf\n", *a, *b, *c );


  return 0;
}
